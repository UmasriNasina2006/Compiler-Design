%{
int chars=0, words=0, lines=0;
%}

%%
[ \t]+        { chars += yyleng; }
\n            { lines++; chars++; }
[A-Za-z0-9]+  { words++; chars += yyleng; }
.             { chars++; }
%%

int main() {
    printf("Enter String:\n");
    yylex();
    printf("Characters=%d\nWords=%d\nLines=%d\n", chars, words, lines);
}
int yywrap() { return 1; }
